---
phase: 07-landing-page-and-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/layout/sidebar.tsx
autonomous: false
requirements:
  - MKT-01
  - MKT-02

must_haves:
  truths:
    - "Visiting / shows a public marketing page — not a redirect to /login"
    - "Authenticated users visiting / are redirected to /dashboard"
    - "Landing page has hero section with headline, subheadline, and Get Started CTA"
    - "Landing page has features section highlighting AI-powered, branded, and LinkedIn-ready"
    - "Landing page has pricing section comparing Free (3 carousels/month) vs Pro ($29.99/month, 10 carousels/month)"
    - "Landing page has a nav with Logo, Sign In link, and Get Started Free CTA"
    - "Sidebar no longer shows a disabled 'Generate' Coming Soon item — it was already built in Phase 5"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Public marketing landing page — hero, features, pricing, nav, footer"
      contains: "Get Started"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Cleaned-up sidebar without the stale Coming Soon / Generate disabled item"
  key_links:
    - from: "src/app/page.tsx"
      to: "/signup"
      via: "Get Started Free CTA button"
      pattern: "/signup"
    - from: "src/app/page.tsx"
      to: "/login"
      via: "Sign In nav link"
      pattern: "/login"
---

<objective>
Build the public marketing landing page at `/` and clean up the sidebar stale copy. The landing page converts visitors to sign-ups and uses the same aesthetic as the auth pages (zinc-950 dark sections + white light sections, amber-500 accent, Framer Motion fade-ins).

Purpose: Right now `/` redirects to `/login`, which means there's no public marketing presence. This plan replaces that redirect with a real landing page.

Output: `src/app/page.tsx` becomes a multi-section Server Component landing page. `src/components/layout/sidebar.tsx` has the "Coming Soon → Generate" disabled item removed (it was built in Phase 5).
</objective>

<execution_context>
@C:/Users/APC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/APC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx
@src/app/(auth)/login/page.tsx
@src/components/layout/sidebar.tsx

<interfaces>
<!-- Design system (established across auth pages and app) -->
// Dark sections: bg-zinc-950, text-white, amber-400 accent on dark
// Light sections: bg-white or bg-gray-50, text-gray-900, amber-500/600 accent
// Cards: bg-white border border-gray-200 shadow-sm rounded-2xl
// Buttons primary (dark bg): bg-gray-900 text-white hover:bg-gray-800 rounded-xl px-5 py-2.5 text-sm font-semibold
// Buttons primary (amber): bg-amber-500 text-white hover:bg-amber-400 rounded-xl
// Typography: font-bold tracking-tight for headings, text-gray-500 for body
// Section tags: text-[10px] font-bold uppercase tracking-widest text-amber-600
// Framer Motion: initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.4 }}
// Logo: 2×2 grid of squares SVG in gray-900 bg rounded-lg (same as sidebar)

<!-- Pricing (from PROJECT.md / Phase 3) -->
// Free: 3 carousels/month, $0
// Pro: 10 carousels/month, $29.99/month

<!-- CarouselPreview component from login/page.tsx -->
// Already built as an inline component in login/page.tsx — copy or extract for reuse
// Shows a LinkedIn-style post card mockup with slide thumbnails

<!-- Auth routing -->
// Middleware does NOT protect "/" — landing page is public
// Authenticated check: import createClient from supabase/server, call getUser()
// If user exists → redirect('/dashboard')
// This must be done in a Server Component (page.tsx)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace page.tsx redirect with full landing page</name>
  <files>src/app/page.tsx</files>
  <action>
Completely rewrite `src/app/page.tsx`. The current file is just `redirect("/login")` — replace it with a full Server Component landing page.

**Auth gate first** — If user is logged in, redirect to `/dashboard`:
```typescript
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export default async function LandingPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (user) redirect('/dashboard')

  return <LandingPageContent />
}
```

Separate the content into a `LandingPageContent` function (or keep inline — since no client hooks needed, a single Server Component is fine).

**Page structure — all sections in order:**

---

### Section 1: Nav
Sticky top nav, `sticky top-0 z-50 bg-white/90 backdrop-blur-sm border-b border-gray-100`:
- Left: Logo (2×2 squares SVG in bg-gray-900 rounded-lg) + "VSS Creator" text
- Right: "Sign in" link (text-gray-600 hover:text-gray-900 text-sm font-medium) + "Get started free" button (bg-gray-900 text-white rounded-xl px-4 py-2 text-sm font-semibold hover:bg-gray-800)
- Container: `max-w-6xl mx-auto px-6 h-16 flex items-center justify-between`

```tsx
const LogoIcon = () => (
  <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
    <rect x="1" y="1" width="6" height="6" rx="1.5" />
    <rect x="9" y="1" width="6" height="6" rx="1.5" opacity="0.4" />
    <rect x="1" y="9" width="6" height="6" rx="1.5" opacity="0.4" />
    <rect x="9" y="9" width="6" height="6" rx="1.5" opacity="0.15" />
  </svg>
)
```

---

### Section 2: Hero
Dark section using zinc-950 background (matching the login page left panel aesthetic):
```
bg-zinc-950 relative overflow-hidden
```

Layout: two-column on desktop (`lg:grid lg:grid-cols-2 lg:gap-12 items-center`), stacked on mobile.

Left column (copy):
- Eyebrow tag: `text-[11px] font-bold uppercase tracking-widest text-amber-400 mb-4`  → "LinkedIn Content Creator"
- Headline: `text-4xl lg:text-5xl font-bold text-white leading-[1.1] tracking-tight`
  → "Turn ideas into viral LinkedIn carousels in under a minute"
- Sub: `text-zinc-400 text-base lg:text-lg leading-relaxed mt-4 max-w-md`
  → "AI-powered carousel generation with your brand's voice, colors, and style — no design tool required."
- CTA row: two buttons stacked horizontally:
  - Primary: `bg-amber-500 text-white hover:bg-amber-400 rounded-xl px-6 py-3 text-sm font-semibold inline-flex items-center gap-2` → "Get started free →"
  - Secondary: `text-zinc-400 hover:text-white text-sm font-medium` → "Sign in"
- Pill badges below (same as login): `['AI-powered', 'LinkedIn ready', 'No design skills', 'Your brand voice']`
  → `text-xs text-zinc-300 bg-white/10 border border-white/10 rounded-full px-3 py-1`

Right column (carousel preview mockup):
Copy the `CarouselPreview` component from `src/app/(auth)/login/page.tsx` exactly as-is into this file. Do NOT import from login — just duplicate the JSX inline as a local function `function CarouselPreview()`.

Decorative orbs (same as login):
```tsx
<div className="pointer-events-none absolute -top-32 -left-32 w-[500px] h-[500px] rounded-full bg-violet-600/15 blur-3xl" />
<div className="pointer-events-none absolute -bottom-40 -right-20 w-[400px] h-[400px] rounded-full bg-indigo-600/15 blur-3xl" />
```

---

### Section 3: How it works
Light section — `bg-white py-24`:
- Container: `max-w-6xl mx-auto px-6`
- Eyebrow: `text-[11px] font-bold uppercase tracking-widest text-amber-600` → "How it works"
- Heading: `text-3xl font-bold text-gray-900 tracking-tight mt-2` → "Three steps to your next post"
- 3-step grid: `grid grid-cols-1 md:grid-cols-3 gap-8 mt-12`

Each step card (`bg-gray-50 rounded-2xl p-6 border border-gray-100`):
- Step number: `text-[11px] font-bold uppercase tracking-widest text-amber-600` → "Step 1"
- Icon: simple inline SVG or lucide icon (use `Lightbulb`, `Sparkles`, `Download` from lucide-react)
- Title: `text-lg font-semibold text-gray-900 mt-3`
- Body: `text-sm text-gray-500 mt-2 leading-relaxed`

Step content:
1. **Describe your idea** (Lightbulb icon) → "Enter your topic or paste your raw thoughts. No need to structure it — AI handles that."
2. **AI generates your carousel** (Sparkles icon) → "Pick your template, image style, and brand. One click generates slides with AI-written copy and visuals."
3. **Download and post** (Download icon) → "Export individual slides as PNG or download a full PDF carousel ready for LinkedIn."

---

### Section 4: Features
Light gray section — `bg-gray-50 py-24`:
- Container: `max-w-6xl mx-auto px-6`
- Eyebrow: "Why VSS Creator"
- Heading: "Everything you need to create at scale"
- `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-12`

Feature cards (`bg-white rounded-2xl p-6 border border-gray-200 shadow-sm`):
- Icon wrapper: `w-10 h-10 rounded-xl bg-amber-50 flex items-center justify-center mb-4`
- Icon: lucide-react, text-amber-600, w-5 h-5
- Title: `text-base font-semibold text-gray-900`
- Body: `text-sm text-gray-500 mt-1.5 leading-relaxed`

6 features:
1. **Your brand, always** (Palette icon) → "Set your brand colors, voice guidelines, and audience once. Every carousel reflects your identity automatically."
2. **5+ templates** (LayoutTemplate icon) → "Choose from professionally designed carousel layouts — cover page, content slides, and a CTA slide included."
3. **AI-generated visuals** (Image icon) → "DALL-E 3 generates on-brand images for each slide based on your content and style choice."
4. **LinkedIn-ready captions** (MessageSquare icon) → "Get a full post body with your carousel — written in your brand's voice and optimized for LinkedIn engagement."
5. **Download in seconds** (Download icon) → "Export individual slides as PNG or a full PDF ready to upload to LinkedIn. No extra tools needed."
6. **Credit-based pricing** (Zap icon) → "Pay only for what you use. Free plan includes 3 carousels per month. Upgrade to Pro for 10 per month."

Import all needed icons from lucide-react: `Palette, LayoutTemplate, Image, MessageSquare, Download, Zap, Lightbulb, Sparkles`.

---

### Section 5: Pricing
White section — `bg-white py-24`:
- Container: `max-w-4xl mx-auto px-6`
- Eyebrow: "Pricing"
- Heading: "Simple, transparent pricing"
- Sub: `text-gray-500 text-base mt-2` → "Start free. Upgrade when you're ready."
- Grid: `grid grid-cols-1 md:grid-cols-2 gap-6 mt-12 max-w-2xl mx-auto`

**Free card** (`bg-gray-50 rounded-2xl p-8 border border-gray-200`):
- Badge: `text-[10px] font-bold uppercase tracking-widest text-gray-500` → "Free"
- Price: `text-4xl font-bold text-gray-900 mt-2` → "$0" + `text-gray-500 text-base font-normal` → " / month"
- Divider
- Feature list (`space-y-3 mt-6`), each item: checkmark (✓ in text-green-600) + `text-sm text-gray-600`
  - 3 carousels per month
  - All templates
  - AI-generated images
  - LinkedIn caption included
  - PNG & PDF export
- CTA: `w-full mt-8 rounded-xl border border-gray-300 bg-white text-gray-900 hover:bg-gray-50 px-4 py-2.5 text-sm font-semibold text-center block` → "Get started free"

**Pro card** (`bg-gray-900 rounded-2xl p-8 border border-gray-800 relative overflow-hidden`):
- "Most popular" badge: `absolute top-4 right-4 text-[10px] font-bold uppercase tracking-widest text-amber-400 bg-amber-400/10 border border-amber-400/20 rounded-full px-3 py-1`
- Badge: `text-[10px] font-bold uppercase tracking-widest text-amber-400` → "Pro"
- Price: `text-4xl font-bold text-white mt-2` → "$29.99" + `text-zinc-400 text-base font-normal` → " / month"
- Divider (`border-zinc-800`)
- Feature list, each item: checkmark (text-amber-400) + `text-sm text-zinc-300`
  - **10 carousels per month**
  - All templates
  - AI-generated images
  - LinkedIn caption included
  - PNG & PDF export
  - Priority processing
- CTA: `w-full mt-8 rounded-xl bg-amber-500 text-white hover:bg-amber-400 px-4 py-2.5 text-sm font-semibold text-center block transition-colors` → "Upgrade to Pro →"
  Link to `/signup`

---

### Section 6: CTA Banner
Dark section — `bg-zinc-950 py-20`:
- Centered: `max-w-2xl mx-auto px-6 text-center`
- Eyebrow: `text-amber-400` → "Get started today"
- Heading: `text-3xl lg:text-4xl font-bold text-white tracking-tight mt-2` → "Your next viral post starts with one idea"
- Sub: `text-zinc-400 text-base mt-4` → "Join creators who are shipping LinkedIn content faster with AI."
- CTA button: `bg-amber-500 text-white hover:bg-amber-400 rounded-xl px-8 py-3 text-sm font-semibold inline-flex items-center gap-2 mt-8` → "Create your first carousel →"
  Link to `/signup`

---

### Section 7: Footer
Simple footer — `border-t border-gray-200 bg-white py-8`:
- Container: `max-w-6xl mx-auto px-6 flex flex-col sm:flex-row items-center justify-between gap-4`
- Left: Logo + "VSS Creator" text
- Right: `text-sm text-gray-400` → "© 2026 VSS Creator. All rights reserved."

---

**Animation note:** Since this is a Server Component, use CSS classes only (no `motion.*`). Use Tailwind's built-in animation utilities or `tw-animate-css` if needed. Do NOT add `'use client'` for animations — a static page with no JS is fine. If you want scroll-triggered animations, skip them for now — the page is functional without them.

**No new dependencies.** Use only: lucide-react (already installed), next/link, next/navigation, @/lib/supabase/server.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "app/page" || echo "no TS errors in landing page"</automated>
  </verify>
  <done>
    - src/app/page.tsx is a Server Component landing page (not a redirect)
    - Authenticated users are redirected to /dashboard
    - Page has nav, hero, how-it-works, features, pricing, CTA banner, footer sections
    - All lucide-react icons are from the installed package
    - No new dependencies introduced
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean up sidebar — remove stale Coming Soon / Generate disabled item</name>
  <files>src/components/layout/sidebar.tsx</files>
  <action>
Read `src/components/layout/sidebar.tsx` first. Find the "Coming Soon" section at the bottom of the nav that contains a disabled `NavItem` for "Generate" and the "Coming Soon" label paragraph. This was placeholder copy from before Phase 5 was built.

Remove the entire block:
```tsx
<p className="px-3 pt-2 pb-1.5 text-[10px] font-semibold uppercase tracking-widest text-gray-300">
  Coming Soon
</p>
<NavItem
  href="#"
  icon={<Sparkles className="w-4 h-4 flex-shrink-0" />}
  label="Generate"
  disabled
/>
```

Also update the footer version text from `v0.1 &mdash; pre-launch` to `v1.0`.

Remove the `Sparkles` icon import from lucide-react if it's no longer used anywhere else in the file (check — `Sparkles` was only used in the Coming Soon disabled item).

Do NOT change any other nav items, layout, or styling.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "sidebar" || echo "no TS errors in sidebar"</automated>
  </verify>
  <done>Coming Soon section and disabled Generate nav item removed from sidebar. Version updated to v1.0. TypeScript compiles without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Public landing page at / with nav, hero (dark zinc-950), how-it-works, features, pricing (Free vs Pro), CTA banner, and footer. Authenticated users are redirected to /dashboard. Sidebar Coming Soon / Generate disabled item removed, version updated to v1.0.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000 in a browser (logged out / incognito)
    2. Verify the landing page renders — NOT a redirect to /login
    3. Verify the nav shows Logo, "Sign in" link, and "Get started free" button
    4. Verify the hero section uses a dark zinc-950 background with the carousel preview mockup on the right (desktop)
    5. Verify the Features section shows 6 feature cards with icons
    6. Verify the Pricing section shows Free ($0) and Pro ($29.99/month) cards — Pro card is dark with amber accent
    7. Click "Get started free" — should navigate to /signup
    8. Click "Sign in" — should navigate to /login
    9. Log in as an existing user and visit http://localhost:3000 — should redirect to /dashboard automatically
    10. Navigate to /dashboard (logged in) and verify the sidebar no longer shows "Coming Soon" or the disabled Generate item
    11. Verify the sidebar footer shows "v1.0"
    12. Run `npx tsc --noEmit` — should pass with no errors
  </how-to-verify>
  <resume-signal>Type "approved" when the landing page looks correct and sidebar is clean, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` completes with no errors
- Unauthenticated visit to / renders landing page sections (not a redirect)
- Authenticated visit to / redirects to /dashboard
- All CTA links point to /signup or /login (correct)
- Sidebar has no Coming Soon or disabled Generate item
</verification>

<success_criteria>
MKT-01: Public landing page is live with product copy, pricing comparison (Free vs Pro), and CTAs that link to the sign-up flow.
MKT-02 (partial): Landing page uses the Resume.io aesthetic — modern minimalist, clean typography, amber accent, zinc-950 dark hero section matching the existing auth page style.
</success_criteria>

<output>
After completion, create `.planning/phases/07-landing-page-and-polish/07-01-SUMMARY.md`
</output>
