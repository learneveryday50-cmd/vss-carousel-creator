---
phase: 04-n8n-workflow-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-n8n-workflow-migration/vss-carousel-workflow-v6.json
autonomous: false
requirements:
  - N8N-01
must_haves:
  truths:
    - "A complete n8n workflow JSON file exists that can be imported directly into n8n Cloud"
    - "The workflow receives the Next.js webhook payload, sends a 202 ACK immediately, then sets carousel status to processing"
    - "Gemini 2.0 Flash is called with multimodal input (idea_text + brand context + template_front_url image + style_url image) and produces exactly slide_count slides with visual_prompt per slide plus post_body — handles both customers with custom templates and those using catalog defaults"
    - "Each slide goes through DALL-E 3 image generation followed by ImgBB upload producing a public URL"
    - "All slide URLs are collected in order and PATCHed to Supabase carousels table with status=completed and post_body"
    - "Any node failure triggers a PATCH to Supabase with status=failed"
    - "Supabase writes use HTTP Request node with apikey header credential only (not the native Supabase node)"
  artifacts:
    - path: ".planning/phases/04-n8n-workflow-migration/vss-carousel-workflow-v6.json"
      provides: "Importable n8n Cloud workflow JSON — the complete carousel generation pipeline"
      contains: "nodes, connections, webhook trigger, Gemini call, SplitInBatches loop, DALL-E 3, ImgBB upload, Supabase PATCH"
  key_links:
    - from: "Webhook node"
      to: "Respond to Webhook (202 ACK)"
      via: "parallel branch — ACK fires before AI processing begins"
      pattern: "respondMode: responseNode"
    - from: "Gemini HTTP Request node"
      to: "Code node (parse slides)"
      via: "JSON response body → slides array"
      pattern: "JSON.parse($json.candidates[0].content.parts[0].text)"
    - from: "SplitInBatches node"
      to: "DALL-E 3 HTTP Request"
      via: "one slide item per iteration"
      pattern: "$json.visual_prompt → image generation request"
    - from: "Aggregate node"
      to: "PATCH Supabase completed HTTP Request"
      via: "ordered slide_urls array + post_body from Set node"
      pattern: "PATCH /rest/v1/carousels?id=eq.{{ $('Webhook').item.json.carousel_id }}"
---

<objective>
Build a complete n8n Cloud workflow JSON from scratch that implements the full carousel generation pipeline: webhook → 202 ACK + set processing → Gemini 2.0 Flash (multimodal, reads style_url) → parse slides → SplitInBatches loop (DALL-E 3 + ImgBB per slide) → aggregate URLs → PATCH Supabase completed. Error path PATCHes Supabase failed.

Purpose: Phase 5 (Generation Dashboard) is already complete and polls Supabase for carousel status. This workflow is the backend that actually runs the AI generation and writes results back to Supabase. Without this workflow active in n8n Cloud, the UI shows "processing" forever.

Output: vss-carousel-workflow-v6.json — a valid, importable n8n workflow JSON file. User imports it, sets credentials, activates it, runs a smoke test.
</objective>

<execution_context>
@C:/Users/APC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/APC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-n8n-workflow-migration/04-CONTEXT.md
@.planning/phases/04-n8n-workflow-migration/04-RESEARCH.md
</context>

<interfaces>
<!-- What Next.js sends to n8n (from src/app/api/generate/route.ts) -->
<!-- Carousel row is pre-created with status='processing' BEFORE the webhook fires -->
<!-- n8n only needs to PATCH the existing row — no INSERT needed -->

```json
{
  "carousel_id": "uuid",
  "idea_text": "string",
  "slide_count": 7,
  "brand_id": "uuid",
  "brand_name": "string",
  "brand_color": "#hex",
  "voice_guidelines": "string",
  "product_description": "string",
  "audience_description": "string",
  "cta_text": "string",
  "template_id": "uuid",
  "template_name": "string",
  "template_slug": "string",
  "template_front_url": "string",
  "template_content_url": "string",
  "template_cta_url": "string",
  "image_style_id": "uuid",
  "style_name": "string",
  "style_description": "string",
  "style_url": "string",
  "hook_style_name": "string",
  "hook_style_description": "string",
  "hook_style_instruction": "string",
  "design_style": "string",
  "design_style_description": "string",
  "design_style_url": "string",
  "custom_instructions": ""
}
```

<!-- What n8n must write to Supabase carousels -->
<!-- PATCH /rest/v1/carousels?id=eq.{carousel_id} -->
```json
{
  "status": "completed",
  "post_body": "LinkedIn post text string",
  "slide_urls": ["url1", "url2", "..."],
  "updated_at": "ISO timestamp"
}
```

<!-- Supabase auth pattern (CRITICAL — do not deviate) -->
<!-- HTTP Request node with Header Auth credential (apikey header) -->
<!-- ALSO add manual Authorization: Bearer header with same service_role value -->
<!-- Add Prefer: return=representation to get 200 JSON back (not 204 which breaks n8n) -->
<!-- Do NOT use the native n8n Supabase node — GitHub issue #17020 causes 403 -->

<!-- n8n existing credentials (use these, do not create new) -->
<!-- - Supabase credential id: 4wAFnVPwpyyeSCk6 (Header Auth, apikey header) -->
<!-- - Google Gemini credential: available in instance -->
<!-- - OpenAI credential: available in instance -->

<!-- Webhook ID to reuse: 601ee2d2-2ac6-48c1-8d49-8bd94875aeb4 -->
<!-- Webhook URL pattern: https://ovobvc.ezn8n.com/webhook/601ee2d2-2ac6-48c1-8d49-8bd94875aeb4 -->
<!-- Next.js sends X-Webhook-Secret header — validate it in the workflow -->

<!-- ImgBB upload: multipart POST to https://api.imgbb.com/1/upload?key={IMGBB_API_KEY} -->
<!-- Field: image = base64 string (NOT data URI — raw base64 only) -->
<!-- Response: data.url = public image URL -->

<!-- DALL-E 3 call: POST https://api.openai.com/v1/images/generations -->
<!-- Body: { model: "dall-e-3", prompt: "...", n: 1, size: "1024x1024", response_format: "b64_json" } -->
<!-- Response: data[0].b64_json = base64 image string -->
<!-- Auth: Authorization: Bearer {OPENAI_API_KEY} -->

<!-- Gemini call: POST https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key={GOOGLE_AI_API_KEY} -->
<!-- For multimodal (reading style_url image): include inlineData or fileData part -->
<!-- Response: candidates[0].content.parts[0].text = JSON string -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the complete n8n workflow JSON</name>
  <files>.planning/phases/04-n8n-workflow-migration/vss-carousel-workflow-v6.json</files>
  <action>
    Write the complete n8n workflow JSON to the file path above. This is a hand-crafted JSON that will be imported directly into n8n Cloud. The file must be valid JSON matching n8n's workflow import schema.

    NODE LIST AND POSITIONS (write all nodes in the nodes array):

    1. Webhook (n8n-nodes-base.webhook)
       - id: "node-webhook"
       - webhookId: "601ee2d2-2ac6-48c1-8d49-8bd94875aeb4"
       - path: "601ee2d2-2ac6-48c1-8d49-8bd94875aeb4"
       - httpMethod: "POST"
       - responseMode: "responseNode" (sends ACK via Respond node, not automatically)
       - authentication: "headerAuth" with header name "X-Webhook-Secret"
       - position: [0, 0]

    2. Respond to Webhook — 202 ACK (n8n-nodes-base.respondToWebhook)
       - id: "node-ack"
       - responseCode: 202
       - responseBody: '{"status":"accepted"}'
       - respondWith: "json"
       - position: [250, -150]
       - Connected FROM: Webhook (parallel with node-set-processing)

    3. PATCH Supabase — Set Processing (n8n-nodes-base.httpRequest)
       - id: "node-set-processing"
       - method: "PATCH"
       - url: "https://ihtowlmrgjhgwgnxgimy.supabase.co/rest/v1/carousels?id=eq.={{ $json.carousel_id }}"
       - authentication: "predefinedCredentialType" → "httpHeaderAuth" → credentialId: "4wAFnVPwpyyeSCk6"
       - sendHeaders: true — headers array:
           { name: "Content-Type", value: "application/json" }
           { name: "Authorization", value: "Bearer ={{ $credentials.httpHeaderAuth.value }}" }
           { name: "Prefer", value: "return=representation" }
       - sendBody: true, contentType: "json"
       - body: { "status": "processing", "updated_at": "={{ new Date().toISOString() }}" }
       - position: [250, 150]
       - Connected FROM: Webhook (parallel with node-ack)
       - NOTE: The Authorization Bearer value must reference the credential value dynamically.
         If the credential expression is unavailable, use a hardcoded placeholder "SUPABASE_SERVICE_ROLE_KEY_PLACEHOLDER"
         and note in the task output that the user must replace it with their actual service_role JWT.

    4. Gemini — Generate Slide Content (n8n-nodes-base.httpRequest)
       - id: "node-gemini"
       - method: "POST"
       - url: "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent"
       - authentication: "genericCredentialType" → "httpQueryAuth" (uses Google AI API key as query param "key")
         OR: sendQuery: true with query param name="key", value={{ $credentials.googlePalmApi.apiKey }} if Google Gemini credential is available
         FALLBACK: hardcode placeholder URL suffix "?key=GOOGLE_AI_API_KEY_PLACEHOLDER"
       - sendBody: true, contentType: "json"
       - body (as JSON expression): Build the Gemini request body with multimodal content:
         {
           "contents": [{
             "parts": [
               {
                 "text": "You are a LinkedIn carousel content creator. Generate exactly {{ $('Webhook').item.json.slide_count }} slides for a LinkedIn carousel and a LinkedIn post body.\n\nBrand: {{ $('Webhook').item.json.brand_name }}\nBrand Color: {{ $('Webhook').item.json.brand_color }}\nVoice: {{ $('Webhook').item.json.voice_guidelines }}\nProduct: {{ $('Webhook').item.json.product_description }}\nAudience: {{ $('Webhook').item.json.audience_description }}\nCTA: {{ $('Webhook').item.json.cta_text }}\n\nCarousel Idea: {{ $('Webhook').item.json.idea_text }}\n\nHook Style: {{ $('Webhook').item.json.hook_style_name }} — {{ $('Webhook').item.json.hook_style_instruction }}\nDesign Style: {{ $('Webhook').item.json.design_style }} — {{ $('Webhook').item.json.design_style_description }}\nImage Style: {{ $('Webhook').item.json.style_name }} — {{ $('Webhook').item.json.style_description }}\n{{ $('Webhook').item.json.custom_instructions ? 'Custom Instructions: ' + $('Webhook').item.json.custom_instructions : '' }}\n\nTwo reference images are provided (when available):\n- IMAGE 1 (Template): The slide template/layout the customer uses. Use its structural layout, spacing, color palette, and typography mood as the foundation.\n- IMAGE 2 (Style): The artistic/visual style for the illustrations. Mimic its texture, rendering technique, and mood.\n\nIf a customer has their own custom template (IMAGE 1 is their branded design), write DALL-E prompts that closely match that template's visual identity. If IMAGE 1 is a default catalog template, use it as loose structural inspiration only.\n\nWrite DALL-E prompts that blend both references — template structure from IMAGE 1, artistic style from IMAGE 2.\n\nRespond ONLY with valid JSON matching this exact schema (no markdown, no explanation):\n{\n  \"post_body\": \"LinkedIn post text (engaging, 150-300 words, includes hook and CTA)\",\n  \"slides\": [\n    {\n      \"index\": 1,\n      \"heading\": \"Slide heading (short, punchy, max 8 words)\",\n      \"body\": \"Slide body text (2-4 sentences)\",\n      \"visual_prompt\": \"Detailed DALL-E image generation prompt describing the visual for this slide — blend template structure with artistic style\"\n    }\n  ]\n}\n\nYou MUST produce exactly {{ $('Webhook').item.json.slide_count }} slide objects. No more, no less."
               },
               {
                 "fileData": {
                   "mimeType": "image/jpeg",
                   "fileUri": "={{ $('Webhook').item.json.template_front_url }}"
                 }
               },
               {
                 "fileData": {
                   "mimeType": "image/jpeg",
                   "fileUri": "={{ $('Webhook').item.json.style_url }}"
                 }
               }
             ]
           }],
           "generationConfig": {
             "temperature": 0.7,
             "responseMimeType": "application/json"
           }
         }
       - IMPORTANT: Both fileData parts use public URLs that Gemini fetches directly.
         - template_front_url is ALWAYS non-empty (brand custom URL OR catalog template cover URL from the app)
         - style_url is ALWAYS non-empty (brand custom OR catalog image style sample URL)
         - No branching needed — Gemini receives both and synthesizes them.
         - Single workflow handles both: customers with own templates (IMAGE 1 = their design) AND customers using default templates (IMAGE 1 = catalog sample).
       - position: [500, 150]
       - Connected FROM: node-set-processing (main output)
       - onError: "continueErrorOutput" (error output connects to node-patch-failed)

    5. Code — Parse Slides JSON (n8n-nodes-base.code)
       - id: "node-parse-slides"
       - language: "javaScript"
       - code:
         ```javascript
         const responseText = $input.item.json.candidates[0].content.parts[0].text;
         let parsed;
         try {
           parsed = typeof responseText === 'string' ? JSON.parse(responseText) : responseText;
         } catch (e) {
           throw new Error('Gemini response was not valid JSON: ' + responseText.substring(0, 200));
         }

         if (!parsed.slides || !Array.isArray(parsed.slides)) {
           throw new Error('Gemini response missing slides array');
         }

         // Store post_body in workflow static data for later use
         $workflow.staticData.post_body = parsed.post_body;
         $workflow.staticData.carousel_id = $('Webhook').item.json.carousel_id;

         // Return each slide as a separate item for SplitInBatches
         return parsed.slides.map(slide => ({ json: slide }));
         ```
       - position: [750, 150]
       - Connected FROM: node-gemini (main output)
       - onError: "continueErrorOutput" (error output connects to node-patch-failed)

    6. SplitInBatches (n8n-nodes-base.splitInBatches)
       - id: "node-split"
       - batchSize: 1
       - position: [1000, 150]
       - Connected FROM: node-parse-slides

    7. DALL-E 3 — Generate Image (n8n-nodes-base.httpRequest)
       - id: "node-dalle"
       - method: "POST"
       - url: "https://api.openai.com/v1/images/generations"
       - authentication: "predefinedCredentialType" → "openAiApi" (uses existing OpenAI credential)
       - sendBody: true, contentType: "json"
       - body:
         {
           "model": "dall-e-3",
           "prompt": "={{ $json.visual_prompt }} -- Style note: branded for {{ $('Webhook').item.json.brand_name }}, color palette anchored to {{ $('Webhook').item.json.brand_color }}. Square format 1:1. Professional LinkedIn carousel slide visual. No text overlays.",
           "n": 1,
           "size": "1024x1024",
           "response_format": "b64_json"
         }
       - position: [1250, 150]
       - Connected FROM: node-split (item output — the loop body)
       - onError: "continueErrorOutput" (error connects to node-patch-failed)

    8. ImgBB — Upload Image (n8n-nodes-base.httpRequest)
       - id: "node-imgbb"
       - method: "POST"
       - url: "https://api.imgbb.com/1/upload"
       - sendQuery: true — query params: { name: "key", value: "IMGBB_API_KEY_PLACEHOLDER" }
         NOTE: User must replace IMGBB_API_KEY_PLACEHOLDER with their actual ImgBB API key after import
       - sendBody: true
       - contentType: "multipart-form-data"
       - bodyParameters:
           { name: "image", value: "={{ $json.data[0].b64_json }}" }
       - position: [1500, 150]
       - Connected FROM: node-dalle (main output)
       - onError: "continueErrorOutput" (error connects to node-patch-failed)

    9. Set — Collect Slide URL (n8n-nodes-base.set)
       - id: "node-collect"
       - mode: "manual"
       - fields:
           { name: "index", value: "={{ $('node-split').item.json.index }}", type: "number" }
           { name: "url", value: "={{ $json.data.url }}", type: "string" }
       - position: [1750, 150]
       - Connected FROM: node-imgbb (main output)
       - Output connects BACK to node-split (loop connection) AND to node-aggregate when loop done

    10. Aggregate — Collect All Slide URLs (n8n-nodes-base.aggregate)
        - id: "node-aggregate"
        - aggregateBy: "aggregateAllItemData"
        - destinationFieldName: "slides"
        - position: [2000, 150]
        - Connected FROM: node-split (done output — the "all items processed" output)

    11. Code — Sort and Build Final Payload (n8n-nodes-base.code)
        - id: "node-build-payload"
        - language: "javaScript"
        - code:
          ```javascript
          const slides = $input.item.json.slides;
          // Sort by index to ensure correct order regardless of execution order
          const sorted = slides.sort((a, b) => a.index - b.index);
          const slideUrls = sorted.map(s => s.url);

          return [{
            json: {
              carousel_id: $workflow.staticData.carousel_id || $('Webhook').item.json.carousel_id,
              post_body: $workflow.staticData.post_body || '',
              slide_urls: slideUrls,
              updated_at: new Date().toISOString()
            }
          }];
          ```
        - position: [2250, 150]
        - Connected FROM: node-aggregate

    12. PATCH Supabase — Set Completed (n8n-nodes-base.httpRequest)
        - id: "node-patch-completed"
        - method: "PATCH"
        - url: "https://ihtowlmrgjhgwgnxgimy.supabase.co/rest/v1/carousels?id=eq.={{ $json.carousel_id }}"
        - authentication: "predefinedCredentialType" → "httpHeaderAuth" → credentialId: "4wAFnVPwpyyeSCk6"
        - sendHeaders: true — headers:
            { name: "Content-Type", value: "application/json" }
            { name: "Authorization", value: "Bearer SUPABASE_SERVICE_ROLE_KEY_PLACEHOLDER" }
            { name: "Prefer", value: "return=representation" }
        - sendBody: true, contentType: "json"
        - body: {
            "status": "completed",
            "post_body": "={{ $json.post_body }}",
            "slide_urls": "={{ JSON.stringify($json.slide_urls) }}",
            "updated_at": "={{ $json.updated_at }}"
          }
        - position: [2500, 150]
        - Connected FROM: node-build-payload

    13. PATCH Supabase — Set Failed (n8n-nodes-base.httpRequest)
        - id: "node-patch-failed"
        - method: "PATCH"
        - url: "https://ihtowlmrgjhgwgnxgimy.supabase.co/rest/v1/carousels?id=eq.={{ $('Webhook').item.json.carousel_id }}"
        - authentication: "predefinedCredentialType" → "httpHeaderAuth" → credentialId: "4wAFnVPwpyyeSCk6"
        - sendHeaders: true — headers:
            { name: "Content-Type", value: "application/json" }
            { name: "Authorization", value: "Bearer SUPABASE_SERVICE_ROLE_KEY_PLACEHOLDER" }
            { name: "Prefer", value: "return=representation" }
        - sendBody: true, contentType: "json"
        - body: { "status": "failed", "updated_at": "={{ new Date().toISOString() }}" }
        - position: [1250, 400]
        - Connected FROM: error outputs of nodes: node-gemini, node-parse-slides, node-dalle, node-imgbb

    CONNECTION MAP (connections object in JSON):
    - node-webhook → [node-ack (output 0), node-set-processing (output 0)]  (parallel)
    - node-set-processing → node-gemini
    - node-gemini main → node-parse-slides
    - node-gemini error → node-patch-failed
    - node-parse-slides main → node-split
    - node-parse-slides error → node-patch-failed
    - node-split item output → node-dalle  (loop body)
    - node-split done output → node-aggregate  (loop complete)
    - node-dalle main → node-imgbb
    - node-dalle error → node-patch-failed
    - node-imgbb main → node-collect
    - node-imgbb error → node-patch-failed
    - node-collect → node-split  (loop back to process next item)
    - node-aggregate → node-build-payload
    - node-build-payload → node-patch-completed

    WORKFLOW JSON TOP-LEVEL STRUCTURE:
    ```json
    {
      "name": "VSS Carousel Creator v6",
      "nodes": [...all 13 nodes...],
      "connections": {...all connections...},
      "active": false,
      "settings": {
        "executionOrder": "v1"
      },
      "versionId": "v6",
      "meta": {
        "templateCredsSetupCompleted": false
      }
    }
    ```

    IMPORTANT NOTES FOR THE WRITTEN JSON:
    - Every node needs: id, name, type, typeVersion, position [x,y], parameters object
    - connections format: { "Source Node Name": { "main": [[{ "node": "Target Node Name", "type": "main", "index": 0 }]] } }
    - For SplitInBatches loop: the "done" output is index 1 in the main array; item output is index 0
    - For error outputs: use "error" key in the connection source: { "main": [...], "error": [[...]] } — but n8n uses onError property on node not connections for error routing; set onError: "continueErrorOutput" on each node that should route errors
    - Credential references in node parameters use: { "id": "4wAFnVPwpyyeSCk6", "name": "Supabase Service Role Key" } in the credentials object of the node
    - The SUPABASE_SERVICE_ROLE_KEY_PLACEHOLDER strings and IMGBB_API_KEY_PLACEHOLDER strings are intentional — the user replaces them post-import (document this in the task output and in a comment at top of the JSON via a "description" field on the workflow)
    - Set workflow "description" to: "VSS Carousel Creator v6 — SETUP REQUIRED: (1) Replace SUPABASE_SERVICE_ROLE_KEY_PLACEHOLDER in nodes 'PATCH Supabase - Set Processing', 'PATCH Supabase - Set Completed', 'PATCH Supabase - Set Failed' with your actual Supabase service_role JWT. (2) Replace IMGBB_API_KEY_PLACEHOLDER in 'ImgBB - Upload Image' query parameter with your ImgBB API key. (3) Ensure OpenAI and Google Gemini credentials are configured."

    Write the complete, valid JSON. Do not abbreviate or truncate — every node must have full parameter definitions.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('.planning/phases/04-n8n-workflow-migration/vss-carousel-workflow-v6.json','utf8')); console.log('nodes:', j.nodes.length, '| name:', j.name, '| has connections:', !!j.connections); if(j.nodes.length < 10) throw new Error('Too few nodes — expected 13');"</automated>
  </verify>
  <done>vss-carousel-workflow-v6.json exists, is valid JSON, contains at least 13 nodes (Webhook, ACK, Set Processing, Gemini, Parse Slides, SplitInBatches, DALL-E, ImgBB, Collect, Aggregate, Build Payload, Set Completed, Set Failed), and has a connections object. File is importable into n8n Cloud.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Import workflow into n8n Cloud, configure credentials, activate, and run smoke test</name>
  <what-built>
    A complete n8n workflow JSON at .planning/phases/04-n8n-workflow-migration/vss-carousel-workflow-v6.json — the full carousel generation pipeline ready to import.
  </what-built>
  <how-to-verify>
    STEP 1 — Import the workflow into n8n Cloud:
    1. Open https://ovobvc.ezn8n.com
    2. Click "Add Workflow" → "Import from File"
    3. Select .planning/phases/04-n8n-workflow-migration/vss-carousel-workflow-v6.json
    4. Confirm the workflow appears with all nodes visible on the canvas

    STEP 2 — Fix the two placeholder values (REQUIRED before activation):
    Search the workflow canvas for any node showing "SUPABASE_SERVICE_ROLE_KEY_PLACEHOLDER":
    - "PATCH Supabase - Set Processing" node → Headers → Authorization value → replace placeholder with your actual Supabase service_role JWT (Supabase Dashboard → Settings → API → service_role)
    - "PATCH Supabase - Set Completed" node → same replacement
    - "PATCH Supabase - Set Failed" node → same replacement
    Search for "IMGBB_API_KEY_PLACEHOLDER":
    - "ImgBB - Upload Image" node → Query Parameters → key → replace with your ImgBB API key

    STEP 3 — Verify credential assignments:
    - "Gemini - Generate Slide Content" node → confirm Google AI / Google Gemini credential is selected
    - "DALL-E 3 - Generate Image" node → confirm OpenAI credential is selected
    - All three Supabase PATCH nodes → confirm "Supabase Service Role Key" (id: 4wAFnVPwpyyeSCk6) credential is assigned

    STEP 4 — Insert a test pending carousel row in Supabase:
    In Supabase Dashboard → Table Editor → carousels, insert a row:
    - id: 00000000-0000-0000-0000-000000000001
    - user_id: your own user UUID (Authentication → Users → copy your UUID)
    - brand_id: any existing brand UUID from your brands table
    - template_id: any existing template UUID from your templates table
    - image_style_id: any existing image_style UUID
    - idea_text: "Test: Benefits of morning routines for productivity"
    - status: "pending"
    - slide_urls: []

    STEP 5 — Run the workflow test:
    Open the workflow in n8n editor (test webhook only works while editor is open).
    Send this payload to the TEST webhook URL (shown in the Webhook node when you click it):
    ```json
    {
      "carousel_id": "00000000-0000-0000-0000-000000000001",
      "idea_text": "Benefits of morning routines for productivity",
      "slide_count": 5,
      "brand_id": "REPLACE_WITH_YOUR_BRAND_UUID",
      "brand_name": "Test Brand",
      "brand_color": "#FF6B00",
      "voice_guidelines": "Professional and clear",
      "product_description": "LinkedIn carousel generator",
      "audience_description": "B2B marketers and entrepreneurs",
      "cta_text": "Book a demo at example.com",
      "template_id": "REPLACE_WITH_YOUR_TEMPLATE_UUID",
      "template_name": "5-Step Framework",
      "template_slug": "five-step-framework",
      "template_front_url": "",
      "template_content_url": "",
      "template_cta_url": "",
      "image_style_id": "REPLACE_WITH_YOUR_IMAGE_STYLE_UUID",
      "style_name": "Technical Annotation",
      "style_description": "Clean technical diagrams with annotations",
      "style_url": "",
      "hook_style_name": "Statistic",
      "hook_style_description": "Opens with a surprising data point",
      "hook_style_instruction": "Start with a compelling statistic that challenges assumptions",
      "design_style": "",
      "design_style_description": "",
      "design_style_url": "",
      "custom_instructions": ""
    }
    ```

    STEP 6 — Check results (generation takes 60-120 seconds):
    In Supabase Table Editor → carousels, refresh and find row id=00000000-0000-0000-0000-000000000001.
    Expected: status="completed", post_body has LinkedIn text, slide_urls has 5 image URLs, updated_at is recent.

    STEP 7 — Activate the workflow:
    Toggle the workflow to "Active" in n8n Cloud. The production webhook URL becomes live.
    Production webhook URL format: https://ovobvc.ezn8n.com/webhook/601ee2d2-2ac6-48c1-8d49-8bd94875aeb4
    Confirm this URL matches the value of N8N_WEBHOOK_URL in your Next.js .env.local.

    Troubleshooting:
    - 403 on Supabase PATCH: Wrong key — confirm service_role JWT (starts with eyJ, 200+ chars, NOT the anon key)
    - Gemini returns non-JSON: Increase temperature or check responseMimeType is "application/json" in Gemini node
    - DALL-E returns error: Check OpenAI credential has billing active and dall-e-3 access
    - ImgBB 400: Confirm image field receives raw base64 string (no "data:image/jpeg;base64," prefix)
    - slide_urls stored as string not array: Check JSON.stringify() is in the body expression for slide_urls field
  </how-to-verify>
  <resume-signal>Type "smoke test passed" if Supabase row shows status=completed with slide_urls and post_body populated. Or describe what failed and at which step.</resume-signal>
</task>

</tasks>

<verification>
Phase 4 is complete when:
1. vss-carousel-workflow-v6.json exists, is valid JSON, passes the node.js parse test in Task 1's automated verify
2. Smoke test confirms: Supabase row id=00000000-0000-0000-0000-000000000001 has status=completed, post_body is a non-empty string, slide_urls contains 5 image URLs
3. n8n workflow is Active in n8n Cloud
4. Production webhook URL matches N8N_WEBHOOK_URL in .env.local

Automated check:
```
node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('.planning/phases/04-n8n-workflow-migration/vss-carousel-workflow-v6.json','utf8')); const names=j.nodes.map(n=>n.name); console.log('Node count:', j.nodes.length); console.log('Nodes:', names.join(', '));"
```
</verification>

<success_criteria>
Phase 4 succeeds when:
- vss-carousel-workflow-v6.json is a valid importable n8n workflow with all 13 pipeline nodes — N8N-01 satisfied
- Triggering the webhook with a test payload results in a Supabase carousels row updating to status=completed with post_body and slide_urls populated
- The workflow is Active in n8n Cloud, ready for Phase 5's fire-and-forget POST to reach it
- Service role key is stored as a named n8n credential, never hardcoded in the JSON (only the placeholder string appears, which the user replaces post-import)
</success_criteria>

<output>
After completion, create .planning/phases/04-n8n-workflow-migration/04-01-SUMMARY.md

Include:
- Workflow structure: list of all 13 nodes by name and purpose
- Production webhook URL (confirmed active)
- Which placeholder values the user replaced (do NOT include actual key values)
- Smoke test result: before/after state of the test Supabase row
- Any deviations from the planned node structure (e.g., if SplitInBatches wiring differed)
- The exact N8N_WEBHOOK_URL value to confirm in .env.local
</output>
