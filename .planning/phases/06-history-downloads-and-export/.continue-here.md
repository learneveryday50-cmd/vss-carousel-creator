---
phase: 06-history-downloads-and-export
task: 0
total_tasks: 6
status: planned
last_updated: 2026-03-17T08:32:16.860Z
---

<current_state>
Phase 6 planning is complete. No execution has started. Both plans (06-01 and 06-02) passed verification and are committed. Ready to execute.
</current_state>

<completed_work>

- Research: DONE — 06-RESEARCH.md written, covers CORS pitfalls, jsPDF SSR, Airtable ID mismatch for brand/template names, existing Phase 5 stubs
- Validation strategy: DONE — 06-VALIDATION.md written (all HIST requirements are manual-only; no test framework in project)
- Plan 06-01: DONE — DB migration + generate route wiring + history page + delete action + CarouselHistory component update
- Plan 06-02: DONE — Image download proxy route + jsPDF PDF export + per-slide download buttons
- Verification: PASSED after 1 revision (checker flagged missing design_style_name column and missing generate route write-path; both fixed)
</completed_work>

<remaining_work>

- Task 0 (Wave 1, Plan 06-01): DB migration — add brand_name, template_name, design_style_name to carousels table
- Task 1 (Wave 1, Plan 06-01): Wire /api/generate/route.ts to write name fields + update creator-workflow.tsx to pass names in POST body
- Task 2 (Wave 1, Plan 06-01): History page server query (extended SELECT) + deleteCarouselAction Server Action
- Task 3 (Wave 1, Plan 06-01): CarouselHistory component — delete UI with optimistic state, brand/template/design pills
- Task 4 (Wave 2, Plan 06-02): Install jsPDF + create /api/download proxy route (auth guard + ImageBB allowlist)
- Task 5 (Wave 2, Plan 06-02): Update CarouselHistory — per-slide download buttons, PDF export, fix CORS on download-all
</remaining_work>

<decisions_made>

- brand_name/template_name/design_style_name stored as denormalized TEXT columns on carousels table — because brand_id/template_id are now Airtable IDs (not Supabase UUIDs), SQL joins are impossible
- Delete action uses createAdminClient() + manual .eq('user_id', user.id) ownership guard — consistent with existing project pattern
- Image downloads route through /api/download proxy — ImageBB CDN blocks direct browser fetch (CORS)
- jsPDF used with dynamic import (await import('jspdf') inside click handler) — jsPDF is not SSR-safe
- No html2canvas needed — slides are already image URLs, not DOM elements
</decisions_made>

<blockers>
- None
</blockers>

<context>
Phase 6 closes the value loop — history, delete, copy, downloads, PDF export. A significant portion was already stubbed in Phase 5 (history/page.tsx and carousel-history.tsx exist). The work is enhancement + completion, not greenfield. The main technical challenges are: (1) CORS-safe image downloads via a server-side proxy, (2) client-side PDF generation with jsPDF, (3) storing brand/template/design style names denormalized at carousel creation time since Airtable IDs replaced Supabase UUIDs.
</context>

<next_action>
Start with: /gsd:execute-phase 6
(Run /clear first for a fresh context window)
</next_action>
